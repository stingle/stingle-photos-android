<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Dark theming is handled by the DayNight base themes in values/styles.xml
        combined with the dark md_* color tokens in values-night/colors.xml.
        Status-bar icon appearance for light/dark is set programmatically with the
        edge-to-edge setup, so no theme overrides are needed here.
    -->
</resources>